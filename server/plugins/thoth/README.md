# Thoth
## Open Metadata Exchange (OME) plugin

An OME plugin defines how to import and publish metadata from an Open Education Resources system.

`plugin.py` provides the class that enables OME to understand Thoth Open Metadata.

This plugin defines one or more:

* `mimetypes` so that Thoth json data can be stored as enclosures in InterNetNews (INN) articles.
* `newsgroups` so Thoth metadata can be published to and subscribed from those INN newsgroups.

The plugin is composed of at least three code files.

* `plugin.py`: Defines `mimetypes`, `newsgroups`, and a `make_metadata_card()` function that translates Thoth data into an OME EducationalResource.
* `thoth_models.py`: Generated by Pydantic tools to make it fast and reliable to import Thoth metadata.
* `bulk_import.py`: Utilities to convert data directly from the Thoth API into OME EducationalResources.

> [!NOTE]
>
> Please ***do NOT edit*** this line and below because when the docs are rebuilt, these lines will be overwritten by scripts/sync_plugin_docs.py.

**MIMETYPES:**
1. application/vnd.thoth.book+json

**NEWSGROUPS:**

{'ome.thoth': 'Metadata from Thoth Open Metadata open access books https://thoth.pub/books'}
```text
server/plugins/thoth
├── __init__.py
├── bulk_import.py
├── plugin.py
├── README.md
├── thoth_item.json
├── thoth_models.py
└── thoth_python_books.json

1 directory, 7 files
```
